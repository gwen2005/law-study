<!DOCTYPE html>  
<html lang="zh-CN">  
<head>  
  <meta charset="UTF-8">  
  <meta name="viewport" content="width=device-width, initial-scale=1.0">  
  <title>{{ title | default('yanziyu.site') }}</title>  
  <style>  
    .disabled {  
      pointer-events: none;  
      opacity: 0.6;  
    }  
  </style>  
</head>  
<body>  
  <script type="text/javascript">  
    var password = prompt("请输入密码:");  
    if (password !== "密码") {  
      document.body.innerHTML = '<h1>密码输入错误</h1><p>该页面现被锁定，请刷新重试</p>';  
      document.title = '404 Not Found';  
      document.body.classList.add('disabled');  
    } else {  
      // 密码正确时的处理逻辑  
    }  
  </script>  
</body>  
</html>

